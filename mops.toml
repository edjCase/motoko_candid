[package]
name = "candid"
version = "2.0.0"
description = "A Candid library for encoding and decoding candid bytes"
repository = "https://github.com/edjcase/motoko_candid"
keywords = ["candid", "encode", "decode"]
license = "MIT"

[dependencies]
core = "0.6.0"
xtended-numbers = "2.0.0"

[dev-dependencies]
test = "2.1.1"
